mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        p=video.description
        if video.hashtags
            ul
                each hashtag in video.hashtags
                    li=hashtag
        else
            p hashtag가 아무것도 없어요
        small=video.createdAt
        hr