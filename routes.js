const HOME = "/";
const USERS = "/users";
const VIDEOS = "/videos";

const JOIN = "/join";
const LOGIN = "/login";
const SEARCH = "/search";

const EDIT = "/edit";
const WATCH = "/watch";

const DELETE = "/delete";
const COMMENT = "/comment";

const routes = {
    home: HOME,
    users: USERS,
    videos: VIDEOS,
    join: JOIN,
    login: LOGIN,
    search: SEARCH,
    watch: WATCH,
    edit: EDIT,
    delete: DELETE,
    comment: COMMENT,
}

export default routes;